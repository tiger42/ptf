#!/bin/bash

DIR=`cd "$(dirname "$0")" && pwd`

cd ${DIR}/../3rdparty/smarty

git init
git remote add origin https://github.com/smarty-php/smarty.git
git fetch origin master
git reset --hard HEAD
git checkout -t origin/master
git pull --rebase

